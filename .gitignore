# Dependencies
node_modules/

# Build output (added when we introduce a build step later)
# dist/  — intentionally NOT ignored: we want to ship the single-file build

# OS / editor cruft
.DS_Store
*.log
.vscode/
.idea/

# Local env
.env
.env.local
